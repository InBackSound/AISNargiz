#Выбор текста
Русский текст
Английский (Lorem ipsum)
Собственные символы

#Платёжные системы
Pay via wallet
Pay via bank card
Pay via direct carrier billing
(взято из тега aria-label)



#Заметки
Бывает, тест на кол-во символов даёт ошибку. Это нормально, система не всегда генерирует ожидаемое кол-во символов


#На будущее
переделать подсчёт кол-ва символов, чтобы учитывал абзац отдельно. сейчас калькулятор считает, что кол-во абзацев всегда 1
нажимаь на кнопку соглашения, только есл она появляется

#команды
команда запуска: tsc; npm run test
тестовый полигон: npm run fasttest  //для проверки css/xpath 


#вопросы: 
1) в каком файле писать if(значение из геркина try/false), то. 
2) как сделать if(a===1 && b===2){то...} скрипт не заходит в тело if'а, даже если оба условия выполняются
3) Мне надо получить значение от системы. в зависимости от значения выполнить действие. я писала логику в page(выяснить, нажато или нет): обработка - в steps(галочка уже стоит и надо, чтобы оставалась стоять - не нажимать повторно)
 а) правильно писать логику в этих файлах? в steps пишу высокоуровнево if...->clickonCheckBox()
 б) что за промисы, почему надо так извращаться, чтобы return значение со страницы?
в Page страницы я сделала "unwrapping function calls which returning a promise". можно ли другим способом получить текущее значение со страницы?
вот так я получаю ответ сейчас:
   public async FindoutStatusUppercaseCheckBox() : Promise<string> {
        return new Promise((status) => {   
            this.generatorElements.uppercaseCheckBox.getAttribute('checked').then(async (value) => {
                await status(value);
            }); 
        });
    }



#заливка в git
открыть консоль в дирректории программы
git status  //посмотреть изменения после последнего коммита
git add .   //добавить всё, кроме игнорируемых
git commit -m "description" 
git push -u origin master

#доп команды в git'е
git rm -r -f --cached .   //удалить все проиндексированные файлы
firefox github.com  //открывать гитхаб



#полезности
Ctrl + H  -  показать скрытые файлы
   


